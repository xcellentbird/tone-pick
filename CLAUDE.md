# tone-pick — 작업 규칙

이 파일은 매 세션 자동으로 읽힌다. **어기면 사고가 나는 것만** 담는다.
배경·구조는 `README.md`, 상세는 `docs/`. 규칙이 테스트나 타입으로 옮겨가면 여기서 지운다.

## 이 앱이 지키는 것

솔로 파티에서 익명으로 '콕' 찌르고, 정해진 시각에 **서로 찌른 쌍만** 공개하는 도구.
판단이 갈리면 아래 셋으로 결정한다.

1. **일방적인 호감은 끝까지 드러나지 않는다** — 거절의 경험을 만들지 않는 게 이 앱의 존재 이유다
2. **운영자만 전체를 본다.** 참가자는 결정된 것만 본다
3. **연락처는 운영자만 본다.** 참가자에게 나가는 건 서로 찌른 쌍의 **실명**까지고,
   한쪽만 찌른 상대에게는 끝까지 익명이다 (ADR-42)

## 절대 규칙

**1. 참가자 응답에 전화번호·인스타를 넣지 마라. 실명은 매칭된 쌍에게만이다**
그리고 **나이·MBTI 는 파티가 시작돼야 나간다** (ADR-21). 명단은 사전 투표부터 열린다 —
화면에서 감추지 말고 `toPublic(player, phase)` 에서 빼라. 개발자 도구를 여는 참가자가 있다.
`toPublic()`을 거친 `PublicPlayer`만 내려간다. 명단(`roster`)에는 예외가 없다.
발표 전에는 콕 발신자(`fromId`)도 응답에 없어야 한다. 개발자 도구로 응답을 여는 참가자가 반드시 있다.

**연락처는 참가자에게 나가지 않는다 — 매칭된 상대에게도, 본인에게도** (ADR-42, ADR-47).
`내 정보` 탭에도 전화번호·인스타 줄을 되살리지 마라 — 번호는 참가자 응답에 **아예 없다**
(`me` 는 `MyProfile` 이다). 인스타는 **고치는 칸에만** 있다.
발표 후 서로 콕 찌른 쌍에게 나가는 건 **실명 하나**다 (`MatchInfo.realName`).
전화번호·인스타는 **운영자만 본다.** `MatchInfo` 에 그 값을 담을 자리가 없는 것이 곧 방어다 —
`contact` 객체를, `phone` 을, `instagram` 을 **다시 만들지 마라.**
한동안 열려 있었고(ADR-19), 얼마나 열지 고르게도 해봤다(ADR-37). 둘 다 걷어냈다.

**인스타를 받는 이유는 운영자 확인 하나다** (ADR-42). 등록 화면이 그렇게 말한다 —
`매칭되면 상대에게 보여요` 를 되살리지 마라. **문구가 코드보다 넓게 말하면 그 순간부터 거짓말이다.**
되살릴 거라면 **내보내는 코드가 먼저**다.
도움말의 `그때 연락처가 열려요` 도 같은 거짓이었다 (ADR-52) — 지웠다.
**없다는 것도 답이라 적는다**: `연락처는 앱이 전하지 않아요 — 그 자리에서 직접 나누세요`.

**2. 파티의 문은 초대 명단이고, 열쇠는 사람마다 다른 링크다** (ADR-32, 슬라이스 13 에서 붙는다)
명단 한 줄마다 고유 토큰이 있고 **`/j/<회차id>/<토큰>` 이 곧 신원**이다.
**참가자는 전화번호를 치지 않는다** — 번호는 남도 알아서 열쇠가 못 된다 (ADR-15 후기 2).

- **링크를 단톡방에 뿌리지 마라.** 사람마다 다르고 1:1 로 보낸다.
  **토큰 없는 `/j/<회차id>` 를 되살리지 마라** — 그게 구멍이었다
- **토큰 재발급 화면을 만들지 마라.** 잃으면 운영자가 안내문을 다시 보낸다.
  정 필요하면 명단에서 빼고 다시 넣으면 새 토큰이 나온다
- **등록 폼에서도 수정 폼에서도 전화번호를 받지 마라.** 번호는 서버가 토큰에서 꺼내
  초대 쿠키에 담는다. 폼에서 받으면 명단에 없는 번호로 바꿔 낼 수 있다 —
  `RegisterInput` 에 phone 키를 두지 않는 것이 그 방어다 (ADR-31)
- 입장 실패 문구는 **하나뿐**이다. "초대되지 않았어요"와 "그런 회차가 없어요"를
  구분해 주면 그 구분이 곧 "이 사람이 이 파티에 있나"의 답이 된다
- 입장 시도 횟수 제한을 빼지 마라. 이 문은 인증 없이 열려 있다
- **명단을 통째로 갈아치우는 API 를 만들지 마라.** 더하기와 빼기만 둔다 —
  한 명 추가하려다 손이 미끄러지면 파티 명단이 전부 날아간다
- 접속지는 해시로만 남긴다. 원본 IP 를 저장하지 마라

**남는 구멍은 "링크를 본 사람"이다.** 친구가 폰에서 보면 그 사람이 될 수 있다 —
알고 고른 대가다 (ADR-32). 사고가 나면 슬라이스 15(비밀번호)를 다시 연다.

**세션은 탭마다 갈린다** (ADR-44). 쿠키는 브라우저 단위라, 링크가 사람마다 달라도
세션이 하나면 **두 번째 탭이 첫 번째 탭을 덮는다.** 그래서 쿠키를 사람마다 따로 둔다 —
`tp_play_<이름표>`, 요청은 `x-tp-ref`, 소켓은 `?ref=`.

- **"이미 등록했나" 를 브라우저 세션에 묻지 마라.** 토큰이 답한 값(`registered`)으로 판정한다 —
  쿠키로 물으면 남의 탭 사람으로 넘어간다. 그게 이 규칙이 생긴 이유다
- **이름표는 비밀이 아니다.** 어느 쿠키를 읽을지 고를 뿐이고 증명은 HttpOnly 쿠키 안에 있다.
  쿠키 이름에 붙는 값이라 **16진수만 통과시킨다** — 안 그러면 `;` 하나로 속성이 붙는다
- 이름표 없는 기본 쿠키를 함께 심는 것을 빼지 마라 — 링크를 닫고 앱 주소만 연 사람의 길이다

**운영자 PIN은 하나뿐이다.** 회차별 PIN을 다시 만들지 마라 (ADR-12) —
두 PIN이 같을 때 회차 담당자가 전체 권한을 얻는 사고가 거기서 나왔다.

**지표는 집계만이다** (ADR-56). 회차 DO 밖에 쌓이는 것에 **사람도 회차도 상대도 없다.**
자리 배정은 **운영자가 손댄 횟수만** 센다 (ADR-58) — 동석률·매칭 쌍 수는 넣지 마라.
상호 쌍이 두셋뿐인 회차에서 그 비율은 **누가 서로 찔렀는지**를 좁히고, 파기 뒤에도 남는다
`pulse()` 에 `eventId` 를 넘길 자리를 만들지 마라 — 인원이 적으면 회차 하나가 곧 사람이고,
지표는 **파기 뒤에도 90일 남는다.** 담기는 값은 `shared/pulse.ts` 의 허용 목록에서만 고른다.
체류 시간은 버킷이고, 라우트는 등록된 패턴이다 — 원본 경로에는 아이디와 토큰이 있다.
**Web Analytics·RUM 을 붙이지 마라**: URL 기준으로 집계해서 `/j/<회차id>/<토큰>` 이 그대로 나간다.

**3. 단계 전환은 서버 시각으로 판단한다**
클라이언트 시계를 쓰면 폰 시간을 바꿔 결과를 먼저 볼 수 있다.
응답에 `x-server-time`을 싣고 클라이언트는 오프셋만 보정한다. `Date.now()`로 마감을 판정하지 마라.

예약이 걸리는 전환은 **매력 투표 시작과 커플 발표** 둘이다. **파티 시작은 운영자가 누른다** (ADR-14).
**등록은 회차를 만드는 순간 열린다** (ADR-38) — 등록 시작 시각을 다시 받지 마라.
시계가 알 수 없는 걸 시계에게 맡기지 마라.

**커플 발표(`revealAt`)는 파티가 시작된 뒤에만 울린다** (ADR-43).
`dueTransition` 의 `phase === "party"` 가 그 약속이다 — **`prevote` 를 거기 더하지 마라.**
아무도 안 온 자리에서 발표가 뜨면 콕이 열린 적도 없어 매칭 0으로 끝난 것이 된다.
그리고 **발표 시각만 파티 시작 뒤에도 고칠 수 있다** — 파티가 길어지면 미뤄야 한다.
전환을 거는 값은 `dueTransition`(판정)과 `nextDue`(알람) **둘이 짝이다.** 조건을 같이 고쳐라 —
한쪽만 고치면 안 울리거나, 울려도 아무 일이 없다.

**매력 투표 마감(`voteEndAt`)은 예약이 아니다** (ADR-39) — 현장이 아니라 **준비**가 쓰는 시각이라
그렇다. 마감돼야 첫 자리를 짤 수 있고, 그 시각과 파티 일시 사이가 짜는 시간이다.
**전환이 아니라 판정이다** — 단계는 `prevote` 그대로고 알람도 울리지 않는다.
단계까지 넘기지 마라. 넘기면 나이·MBTI 와 콕이 함께 열려 빈 자리에서 파티가 시작된다.

**4. 확인 다이얼로그는 무엇이 어떻게 바뀌는지 숫자로 보여준다**
"정말 하시겠습니까?"만 있는 다이얼로그는 만들지 않는다.
되돌릴 수 있는 행동에는 확인을 붙이지 않는다.
**토스트는 화면이 스스로 말하지 못하는 것에만 쓴다** (ADR-65) — 행이 생기거나 숫자가 바뀌면
그것이 곧 알림이다. 토스트는 `fixed` 라 시트 안 목록을 덮는다.
안 보이는 것(복사)은 **누른 그 자리**에서 말한다.

**5. 모달·시트는 라우트다**
뒤로 가기로 닫혀야 한다. **열었다고 입력칸에 커서를 주지 마라** (ADR-63) — 폰에서 키보드가
올라와 화면 절반을 먹는다. `Sheet` 가 막고 있다. 막을 때 `preventDefault()` 만 하면 포커스가
`body` 로 떨어져 트랩이 풀린다 — 시트 자체를 잡아야 한다. 확인 다이얼로그는 실행 **전에** `navigate(-1)` 한다 —
안 그러면 실행 후 뒤로 갔을 때 처리된 다이얼로그가 다시 뜬다. push/replace 표는 `docs/ROUTES.md`.

## 설계 경계

- `EventDO`만 상태를 바꾼다. Worker는 인증·라우팅만 한다
- 참가자 응답을 만드는 곳은 `toPublic()` 한 곳뿐이다
- `buildSeating`은 순수 함수다 — DO·요청·현재시각에 접근하지 마라
- **인터페이스는 구현이 둘 이상일 때만 만든다**

## 확장 제약

나중에 붙일 기능 때문에 지금 추상화하지 마라. 대신 아래만 지킨다.

- 색·간격·모서리는 CSS 변수로만 쓴다. 하드코딩 금지
- 좌석 변경은 `swap(a, b)` 하나뿐. **단일 이동 API를 만들지 마라** — 테이블 인원이 어긋난다
- 참가자에게 알릴 일은 DO의 `broadcast()` 한 곳만 거친다
- 실시간은 **"다시 읽어라" 신호로만** 쓴다. 부분 갱신을 만들면 화면과 서버가 조용히 어긋난다.
  끊겼다 붙었을 때·앱으로 돌아왔을 때도 다시 읽는다 — 놓친 건 서버가 다시 밀어주지 않는다 (ADR-26)
- 참가자 개인정보를 회차 DO 밖으로 복사하지 마라 — 회차 삭제가 DO 하나 지우는 일로 끝나야 한다
- **자동 파기는 없다** (ADR-36). 회차는 운영자가 `이 회차 삭제하기` 를 누를 때까지 남는다.
  **"며칠 뒤에 지워져요" 를 등록 화면에도 운영자 설정에도 다시 적지 마라** — 지우는 코드가 없다.
  되살릴 거라면 **지우는 코드가 먼저**다. 문구가 먼저 가면 그 순간부터 거짓말이다
- 참가자를 지울 때 자리·운세는 지우되 **그가 보낸 콕은 남겨라** (ADR-29) —
  받은 쪽 숫자가 줄면 명단에서 사라진 사람과 맞춰 발신자가 드러난다.
  운세는 반드시 지운다. 그 문장에는 닉네임이 들어 있다
- 지워진 참가자에게 "그런 회차가 없어요" 라고 하지 마라 — 회차는 멀쩡하고 본인이 빠진 것이다

## 테스트

- 테스트는 **규칙이 지켜지는가**에만 쓴다. 함수마다 붙이지 마라
- 테스트가 내부 구조를 알면 안 된다. 공개 표면에만 붙인다 — 리팩터링에 깨지면 테스트를 잘못 쓴 것이다
- 규칙을 새로 만들면 테스트부터 쓴다
- `buildSeating`은 예외 — 순수 함수라 테스트를 먼저 쓰고 구현한다
  (성비 편차 0 / 미배정 0 / 나이차 10살+ 쌍 ≤ N / 나이대 이성 전원 만남 / 마지막 라운드 상호 콕 동석률)
- **자리 배정의 확률적 규칙은 씨앗 하나로 재지 마라** (ADR-57) — 흔들리는 폭이 판정보다 크다.
  씨앗 여러 개를 돌려 평균으로 본다

## 문서 라우팅

| 작업 | 먼저 읽을 것 |
|---|---|
| 무엇을 언제 만드나 | `docs/PLAN.md` — 슬라이스 순서·진행 현황 |
| 지금 슬라이스의 범위 | `docs/scenarios/NN-*.md` — 여기 없는 건 만들지 않는다 |
| 단계·일정·콕·공개 범위 | `docs/DOMAIN.md` |
| 화면을 만든다 | `docs/UI.md` — 화면별 요구사항·모바일 체크리스트 |
| 화면 이동·뒤로 가기 | `docs/ROUTES.md` |
| 문구를 쓴다 | **`src/shared/copy.ts`** — 여기 없으면 여기에 추가한다 |
| 지표를 더한다 | `src/shared/pulse.ts` — 허용 목록. **더하기 전에 ADR-56 · 58** |
| 한쪽 행동이 반대쪽에 어떻게 보이나 | `docs/FLOWS.md` — 상호 영향·엣지 케이스 |
| 자리 배정 | `docs/SEATING.md` — 가중식·불변식·테스트 기준 |
| 이상형 찾기의 얼굴 자료 | `docs/scenarios/19-ideal-type.md` — 자산 계약이 그 안에 있다. 참가자 얼굴은 넣지 않는다 |
| "왜 이렇게 돼 있지?" | `docs/ADR.md` — **바꾸기 전에 반드시.** 머리의 목차에서 찾는다 (폐기된 것도 함께 보인다) |
| 지난 버전에서 뭐가 바뀌었나 | `CHANGELOG.md` — **프로덕션에 나간 것만** 적는다 |

`reference/README.md`에 프로토타입에서 가져올 것과 가져오면 안 되는 것이 정리돼 있다.

## 명령어

```bash
npm run dev:worker   # Worker + DO (127.0.0.1:8787) — 이걸 먼저 띄운다
npm run dev          # Vite. /api, /ws 를 8787 로 프록시
npm run check        # typecheck(클라이언트+Worker+테스트) + 문구·설정·테마·ADR 검사. 커밋 전에 이걸 돌린다
npm test             # workerd 안의 규칙 테스트 + happy-dom 화면 테스트
npm run deploy       # 프로덕션. 비밀값 두 개(MASTER_PIN·SESSION_SECRET)만 넣으면 나간다
npm run deploy:qa    # 연습용. 워커가 달라 회차·참가자 데이터가 완전히 분리된다
                     # (`qa` 브랜치로 PR 을 열면 CI 통과 시 자동 머지 → 자동 배포)
```

**`qa` 로 가는 PR 을 드래프트로 열지 마라.** CI 가 통과하면 자동으로 들어가는 것이
이 저장소가 정한 흐름이고(`ci.yml` 의 `automerge`), 드래프트는 그 자동을 멈춰 세운다 —
사람이 준비됨으로 바꿔야 하고, 그때 CI 가 **한 번 더** 돈다.
**사람이 버튼을 누르는 의식은 `qa → main` 하나뿐이다.**

`npm run check:copy` 는 `copy.ts` 밖에 하드코딩된 한국어를 잡는다.
주석은 통과한다. SQL·정규식처럼 화면 문구가 아닌 건 윗줄에 `copy-ok` 를 적는다.

무료 플랜은 요청당 CPU 10ms다. 자리 배정 로컬 서치만 주의하면 된다.

`node scripts/fortune-spread.mjs <QA주소> --temp <값>` 은 마흔 명분 운세를 뽑아
**서로 얼마나 닮았는지** 잰다 (ADR-60). `LLM_TEMPERATURE` 를 정하는 자리다.
서버가 그 값을 읽으므로 **온도마다 배포가 한 번씩** 필요하지만 PR 은 필요 없다 —
`npx wrangler deploy --env qa --var LLM_TEMPERATURE:1.2`.
판을 모아 `--compare a.json b.json` 하면 온도별 비교표가 나온다.
**규칙문구가 0% 인 줄만 후보다** — 섞이면 유사도가 오히려 올라가서(열다섯 가지뿐) 두 값이 섞인다.
한 번 부른 것은 파일로 남으니 재는 법만 고칠 때는 `--from` 을 쓴다.

**부하 리허설·실험은 QA 에서 한다.** 프로덕션 DO 에 가짜 100명을 넣었다 지우지 마라.
QA 에는 실제 참가자의 전화번호를 넣지 않는다 — 연습용이라고 개인정보가 덜 중요한 게 아니다.

## 자주 틀리는 것

- **새 칸을 가리키는 인덱스를 `SCHEMA` 에 두지 마라.** `CREATE TABLE IF NOT EXISTS` 는
  **이미 있는 표를 건드리지 않아서** 옛 회차에는 그 칸이 없다 — 인덱스가 `no such column` 으로
  던지고, 그 exec 는 try 밖이라 **DO 가 통째로 죽는다.** 칸을 더한 **뒤에** 인덱스를 만든다.
  회차 목록이 모든 회차를 훑기 때문에 화면 하나가 아니라 운영자 콘솔 전체가 멈춘다.
  **테스트는 이걸 못 잡는다** — 테스트 DO 는 언제나 새것이라 옛 표를 만나지 않는다. QA 에서 본다
- 함수 이름을 DOM 빌트인과 겹치게 짓지 마라 — `createEvent`로 버튼이 조용히 죽은 적 있다
- 문구에 "취소"를 쓰지 마라. 옆의 취소 버튼과 헷갈린다 → "되돌리기"
- 받은 콕은 **현황 탭 순위에서만** 보여준다 (ADR-30). 참가자 탭 개인 행·삭제 확인창에는 넣지 마라
- 아바타는 `Avatar` 하나만 쓴다 — **동물 = 사람, 색 = 성별** (ADR-30). 하나가 둘을 말하게 하지 마라
- **한 줄짜리 자리를 새로 만들면 줄 간격을 빼라** (ADR-62). 본문은 1.5 라 배지·칩·숫자·
  탭바처럼 **글이 아니라 손잡이**인 것이 함께 늘어난다. 알약·숫자는 `line-height: 1`,
  **한글이 들어오는 한 줄짜리는 `손잡이 묶음`(1.3)에 이름을 더한다** — 1 로 두면 위아래가
  딱 붙는다. 탭바에 빠뜨렸더니 54px 이 68px 이 됐다.
  반대로 **감싸질 수 있는 것을 손잡이에 넣지 마라** — 여러 줄 칸(`textarea`)과
  길면 두 줄이 되는 질문(`.helpQa .q`)은 본문 간격을 받는다
- 입장 코드는 만든 뒤에 바꾸지 않는다. 수정 경로를 다시 만들지 마라
- **닉네임 칸 안내 문구는 운영자가 적는다** (ADR-59). 기본값 화면에 두고 회차에서 덮어쓴다 —
  위저드에 칸을 되살리지 마라. 그리고 **앱이 고정 문구를 얹지 마라, 기본값도 비워 둬라** —
  우리가 넣은 문장이 붙으면 운영자가 자기가 고른 건지 구분하지 못한다
- 콕 상한을 이미 쓴 횟수 아래로 내리지 못하게 하라 — 남은 횟수가 음수가 된다
- **콕이 오가기 시작하면 대상·되돌리기·알림·일정이 굳는다** (ADR-35, `rulesLocked`).
  회차 설정을 새로 더할 땐 굳는 쪽인지 열린 쪽인지 먼저 정하라 — 콕 횟수만 일부러 열려 있다
- 참가자에게 "0번"이라고 쓰지 마라 — 받은 콕 0회도, 매칭 0명도 별도 문구다
- **닿지 못한 실패(`status 0`)를 서버가 거절한 실패와 같이 다루지 마라.** 401·404 는 다시 물어도
  같은 답이지만 망은 1초 뒤에 멀쩡해진다. **스스로 다시 붙어야 하고, 소켓을 놓으면 안 된다** —
  안드로이드가 화면을 끌 때 무선을 내려서, 켜는 순간의 다시 읽기가 여기 걸린다
- **콕 되돌리기는 회차 설정이 연다** (`allowUndoPre`·`allowUndo`, 라운드마다 따로).
  열린 회차에서는 **다 쓴 뒤에도 창이 열린다** — 되돌리는 길이 그 창 안에만 있어서,
  막아버리면 마지막 한 번을 잘못 쓴 사람이 손쓸 데가 없다
- 받은 콕 알림은 **한 번에 하나씩** 쌓는다. 합쳐서 "N회"로 세지 마라
- 받은 콕에 시각을 붙이지 마라 — 그때 누가 있었는지와 맞추면 발신자가 좁혀진다
- **남이 일으킨 변화에 움직임을 넣지 마라** (ADR-64). 내가 눌러서 생긴 일은 움직여도 되지만,
  broadcast 로 오는 것(받은 콕 배너·목록 갱신·발표 매칭)이 움직이면 **옆 사람이 그 순간을 안다** —
  정지한 글자는 가까이서만 읽히지만 움직임은 테이블 건너편에서도 잡힌다.
  `받은 콕에 시각을 붙이지 마라` 와 같은 누출이다. **탭 전환도 움직이지 마라** — 시트가
  아래에서 올라오는 것이 `이건 닫아야 하는 창` 이라고 말하는데, 탭도 움직이면 그 대비가 사라진다
- 좌석 맞교환은 남녀도 된다 (ADR-16). 다만 **단일 이동 API 는 여전히 만들지 마라**
- 붙어 앉은 쌍을 흩어놓는 기능을 만들지 마라 (ADR-23·49). **섞기도 쌍은 제자리에 둔다** —
  운영자가 손으로 붙여둔 것을 버튼 하나가 헛일로 만들면 안 된다
- **짝을 "한 사람에 하나"로 들지 마라** (ADR-24). A–B, A–C 가 동시에 성립한다 —
  `Map<string, string>` 에 넣으면 앞의 쌍이 조용히 덮인다
- 여러 쌍 중 하나만 붙일 수 있으면 **콕을 더 많이 주고받은 쪽**이다 (ADR-25).
  가중치에는 상한을 둔다 — 한 쌍이 나이차 벌점을 밀어내면 그 테이블만 이상해진다
- **커플 자리 배정을 되살리지 마라** (ADR-51). 배정 버튼은 하나뿐이고, 못 붙은 쌍은
  운영자가 자리에서 `💔` 를 보고 맞교환으로 옮긴다. `final` 을 다시 만들지 마라 —
  참가자 응답에도 넣지 마라. "이번이 커플 자리다" 는 곧 "누가 나를 찔렀나" 의 힌트다
- **`마무리 라운드` 표시를 만들지 마라** (ADR-57). 운영자는 **시계를 보고** 파티를 끝내므로
  "이번이 마지막인가" 에 답할 수 없다. 대신 상호 콕이 **갚은 뒤에도 계속 끌리게** 두면
  새로 만날 사람이 고갈되면서 후반이 저절로 콕 라운드가 된다 (실측 마지막 라운드 97%)
- **`SEAT_W.MUTUAL` 을 8 이상으로 올리지 마라** (ADR-57). 마지막 라운드 동석은 조금 오르는데
  나이대 이성 전원 만남이 무너진다 — 그쪽이 더 많은 사람의 파티를 망친다
- 발표 후에는 콕도, 자리 이동 확인도 뜨지 않는다
- 매칭 표시를 색으로만 하지 마라 — 글자로 같은 정보를 함께 준다
- **도움말에서 자리 이야기는 `내 쪽만` 한다** (ADR-52). `내가 고른 사람과 같은 테이블이 되기
  쉬워져요` 까지다 — `서로 고른 사람끼리` 로 쓰면 내가 안 고른 사람과 같은 테이블일 때
  **그 사람이 나를 골랐다**로 좁혀진다
- **회차 설정을 새로 더하면 도움말이 그 답을 하는지 보라** (ADR-52). 참가자가 겪는 것이
  회차마다 다른데 도움말이 한 가지로만 말하면 그 회차에서는 도움말이 틀린 것이다.
  특히 알림 — 끈 라운드는 **받은 수 자체가 안 내려간다**(`visibleReceived`)
  “아무도 나를 안 골랐다”로 읽히므로 `알려주지 않아요` 라고 적는다
- **어느 프로필에도 전화·인스타를 넣지 마라** (ADR-42). 매칭된 사람에게 나가는 건 실명까지다
- 테이블 수를 설정값으로 만들지 마라 — 자리 배정을 **누를 때** 묻는다 (ADR-28)
- 자리를 확정해도 배정을 닫지 마라 (ADR-28). 발표만이 자리를 끝낸다
- **자리는 발행하면 그때부터 보인다** (ADR-39). 단계로 다시 막지 마라 —
  짜는 동안 새지 않게 하는 일은 **초안**이 이미 맡는다
- 예약 시각을 지우지 마라 — 지나간 예약은 기록으로 남는다
- 등록은 **발표 전까지 계속 열려 있다** — 늦게 온 사람도 파티 중에 합류한다
- 일정 기본값은 파티 일시에서 **거꾸로** 잰다. "만든 지 N시간 뒤"로 되돌리지 마라
- 참가자 카운트다운은 **다음에 일어날 일**을 센다 — 등록 중이면 매력 투표 시작,
  매력 투표 중이면 마감, 마감 뒤에는 파티 시작. 셋 다 일정에 적힌 시각이다.
  **발표는 세지 마라.** 이제 시각은 있지만(ADR-43) 파티 중에 `발표까지 1시간 12분` 이 보이면
  남은 시간을 재며 서두르게 된다 — 이 앱이 만들려는 자리가 아니다
- **알림은 라운드마다 따로다** (ADR-43, `preNotify`·`pokeNotify`). 끄는 건 화면에서 감추는 게
  아니라 `receivedCount` 에서 빼는 일이다 — 그 숫자가 곧 "몇 명이 나를 골랐나" 다.
  총합을 그대로 내려보내면 꺼둔 라운드가 파티 시작과 함께 얹힌다 (`visibleReceived`)

## 오늘의 연애운

- **LLM 에 보내는 값은 `fortuneInput()`·`missionInput()` 이 만드는 것뿐이다** (ADR-20).
  **전화·인스타는 어느 쪽에도 넣지 마라.**
  실명은 **운세 호출 하나에만** 예외다 — 미션 호출에 넣지 마라, 답변에 이름이 나오게 두지 마라,
  저장하지 마라. 셋 중 하나라도 빠지면 예외가 아니라 구멍이다
- **점수·등급·순위를 만들지 마라.** 이 앱이 없애려던 경험을 앱이 직접 만드는 일이다
- 한 번 연 운세를 다시 만들지 마라. 열 때마다 달라지면 그 순간 전부 거짓말이 된다
- LLM 호출을 **DO 안에서 하지 마라** — 그 회차의 모든 요청이 뒤에 선다
- 키가 없어도 화면은 떠야 한다. 규칙 문구 경로를 지우지 마라
- 미션은 30분 안에 되고 **실패해도 티가 나지 않는 것**이어야 한다. "용기를 내세요"는 미션이 아니다
- **모호하게 쓰라고 하지 마라** (ADR-60). 발행 운세가 일부러 모호한 건 혼자 보기 때문이다 —
  여기선 마흔 명이 화면을 나란히 본다. 여지 표현(`~일 수 있어요`)은 **글 전체에 한 번**,
  나머지는 단정한다. 단정 금지는 **앞날**에만 적용된다
- **갈라지는 축은 코드가 준다** (`FORTUNE.tones`, ADR-60). 프롬프트로 "다양하게 쓰세요" 라고
  부탁하지 마라 — 부탁은 지켜지는지 알 수 없고 축은 세어볼 수 있다. **결은 화면에 띄우지 마라**
- **길이는 프롬프트가 정한다. 토큰 상한으로 자르지 마라** — 잘린 JSON 은 조용히 규칙 문구가 된다
- **미션 호출에 운세 본문을 주지 마라** (ADR-60). headline 한 줄이면 된다 —
  본문을 주면 `lead` 가 첫 문단을 유의어로 바꿔 옮겨 적는다. **베낄 것을 안 주는 편이 세다**
- **매력으로 자랑하게 만들지 마라** (ADR-60). 어필에는 실패가 있고 실패는 티가 난다.
  그 매력이 저절로 나올 **자리를 여는** 미션으로 — 상대에게 묻는 모양이면 안 통해도 그냥 잡담이다
- **추론은 미션에만 올린다** (ADR-60). 운세에 올리지 마라 — 거기서 필요한 건 갈라지는 것인데
  **추론은 수렴한다.** `max_completion_tokens` 에 추론 토큰이 함께 들어가는 것도 기억하라

## 아직 만들지 않는 것

프로필 사진, 익명 채팅, UI 테마 선택, 결과 내보내기, 관심사 태그.
로드맵에는 있지만 지금 범위가 아니다. **요청받지 않은 기능을 미리 만들지 마라.**

**푸시 알림은 만들지 마라 — 두 가지가 막는다.**
- **콕은 애초에 못 보낸다.** 잠금화면 알림에는 OS 가 시각을 붙이고 우리가 못 지운다.
  "받은 콕에 시각을 붙이지 마라" 가 그대로 깨진다
- **iOS 는 홈 화면에 추가한 사람만 받는다.** 링크로 들어온 참가자 대부분은 그 단계를 안 밟아서,
  운영자가 믿고 쓸 수 없는 절반짜리 통로가 된다

공지도 푸시로 풀지 마라 (슬라이스 14). **앱으로 돌아올 때 다시 읽는 구조가 이미 그 자리를 맡는다** (ADR-26).

**데모 뷰(가짜 참가자·콕 뿌리기·처음부터·대리 화면)는 걷어냈다** (ADR-7 후기).
운영 앱 안에 시연 도구를 두지 않는다 — 프로덕션 번들에까지 실렸다. 부하 리허설은
`scripts/rehearsal.mjs` 가 실제 등록 경로로 한다.

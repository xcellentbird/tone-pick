# 바뀐 것

이 파일은 **프로덕션에 나간 것**만 적는다. QA 에만 올라간 것은 여기 오지 않는다.
왜 그렇게 정했는지는 `docs/ADR.md`, 지금 규칙은 `CLAUDE.md`.

---

## 2.5.0 — 링크는 하나, 열쇠는 번호와 PIN 번호

큰 판이다. ADR 하나(75)가 **ADR-32 를 뒤집는다.** 운영자 요청 *"사람마다 다른 링크를 하나씩 보내기가
까다롭다"* 에서 시작했다 — 참가자용 단톡방이 없어 안내문과 링크가 한 사람당 두 번씩 갔고, 마흔 명이면
앱 전환 160번이었다. 아직 아무에게도 링크를 보내기 전이라 뒤집는 비용이 0 이었다.

### 1. 참가 링크가 회차마다 하나로 돌아간다 (ADR-75)

`/j/<회차id>` 하나다. **단톡방에 뿌려도 된다** — 링크에는 신원이 없다. 개인별 링크(`/j/<회차id>/<토큰>`)는
걷어냈고 이제 열리지 않는다. 안내문에 `{링크}` 가 돌아와 **안내문 하나가 완결된 초대장**이다 —
전원이 같은 글이라 한 메시지로 끝난다. 명단 행의 링크 버튼은 사라졌고, 링크만 다시 보낼 때 쓰는
**`초대 링크 복사`** 가 명단 머리에 섰다 (주소 하나만, 다른 글자 없이).

### 2. 열쇠는 전화번호 + PIN 번호

배너는 그대로고 버튼 하나(`들어가기`)다. 누르면 **입장 확인창**이 가운데 좁게 뜬다 — 열릴 때 번호 칸에
커서가 있고(ADR-63 의 예외 1호), 11자리가 차면 서버에 묻는다. 아직 등록 전이면 등록 폼으로,
등록한 사람이면 **PIN 번호 칸이 그 아래로 펼쳐지고** 4자리가 차면 낸다. 틀리면 PIN 칸만 비우고
번호는 남는다 — 남은 횟수는 2회부터 말한다. **5회 틀리면 그 번호가 잠기고** 시간이 지나도 안 풀린다.
잠겨도 번호 칸은 산다 (남의 번호를 쳤을 수 있다).

- **실패 문구는 셋이다** — `그런 회차가 없어요` · `초대된 번호가 아니에요` · `PIN 번호가 맞지 않아요`.
  뭉개면 번호를 잘못 친 사람이 PIN 번호를 계속 다시 쳐서 잠금에 걸린다
- **등록은 3걸음** — `기본 정보(인스타 포함) · 나를 소개 · 다시 들어올 때`. PIN 번호는 마지막에
  **두 번** 친다 — 한 번 정하면 못 고쳐서 재입력이 유일한 방어다. 등록을 마쳐야 저장된다
- **약속은 라벨에 붙는다** — `인스타 (운영자 확인용 · 공개되지 않아요)` · `이름 (서로 콕 찌른 상대에게만
  보여요)`. 연락처 걸음이 하던 두 문장이 칸마다 괄호로 옮겨갔다
- **링크를 다시 열면 이 브라우저의 마지막 세션으로 들어간다** — `/` 와 같은 규칙이다. 이 회차의
  세션이 없으면 배너 → 번호 + PIN 번호

### 3. 운영자 — 참가자 PIN 번호는 상태만, 초기화는 한 줄

참가자 상세 시트에 `참가자 PIN 번호` 줄이 생겼다 — `정함 / 안 정함 / 잠김`, 잠김은 눈에 띈다.
**`참가자 PIN 번호 초기화`** 는 PIN 번호와 실패 횟수를 함께 지운다. 새 값은 참가자가 다음 입장에서
정한다(확인창이 칸 둘을 편다). 콕·자리·운세는 그대로다.

PIN 번호는 **해시만** 저장한다 (`HMAC-SHA256`, 후추는 `SESSION_SECRET`) — 값도 해시도 어느 응답에도 없다.
옛 회차에는 `ALTER TABLE` 로 칸이 붙는다. PIN 번호가 생기기 전의 참가자는 다음 입장에서 정한다.

### 그밖에

- **옛 안내문 문구.** 운영자 기본값은 문구를 통째로 저장해서 ADR-32 시절의 글(`함께 보내드린 링크로…`,
  링크 자리 없음)이 레지스트리에 남아 있었다. 옛 기본 문구는 새 기본 문구로 읽고, `{링크}` 자리가 없는
  안내문은 맨 끝 줄에 링크를 붙인다 — 링크 없는 안내문은 나가지 않는다
- **2.4.0 노트의 "아직 정하지 않은 것" 에 답했다.** 첫 화면 예산이 199.6 / 200.0 KiB 였고 입장 확인창이
  1.2 KiB 를 더해 넘었다. 예산을 **216 KiB** 로 올렸다 — 같은 8% 여유다. 이유는 `check-bundle.mjs` 에 있다
- **테스트 41개** — 규칙 24개(`15-pin-entry`)와 화면 17개. `13-personal-link` 는 걷어냈다
- `CLAUDE.md` 절대 규칙 2 를 통째로 다시 썼다. 문서(`ROUTES` · `UI` · `FLOWS` · `PLAN`)도 같은 판에서

### 아직 정하지 않은 것

**첫 입장의 선점.** 아직 등록 전인 남의 번호를 넣고 PIN 번호를 먼저 정할 수 있다 — 그 시점엔 볼 것이 없고
파티 중엔 서로 눈앞에 있어 받아들였다. 사고가 나면 그때 여는 문이 OTP 다 (ADR-75).
번호를 넣어보면 초대 여부를 알 수 있는 것도 알고 고른 대가다 — 뭉개면 잠금 사고가 더 비싸다.
`LLM_TEMPERATURE` 는 2.1.0 에서 적은 그대로 프로덕션에 없다.

---

## 2.4.0 — 같은 테이블 사람이 위로 온다

작은 판이다. ADR 하나(73). 운영자 요청 *"참가자 탭에서 지금 같은 테이블인 사람을 볼 수 있게"*
에서 시작했고, **어디까지 보여주나**를 정하는 데 시간을 썼다 — 이 앱에서 자리는 콕이 끄는 것이라
많이 보여줄수록 *누가 나를 찔렀나* 가 읽힌다.

### 1. 같은 테이블 사람이 참가자 탭 위에 묶인다 (ADR-73)

지금까지 앱은 **누가 내 테이블인지 한 번도 말하지 않았다.** 자리 카드는 번호와 인원 수뿐이었다.
이제 파티 중 자리가 발행돼 있으면, 참가자 탭에서 **같은 테이블 사람이 맨 위에 묶인다** —
머리글 한 줄(`🪑 3번 테이블 · 같이 앉은 사람`)과 그 사람들 아바타의 **테이블 번호 태그**로.
태그는 내 카드의 `나` 와 같은 부품이라 이름 줄은 그대로다.

자리는 방 안에서 이미 보이는 사실이라 앱이 말해도 된다. 지키는 건 **선 셋**이다.

- **이번 라운드만.** 지나온 테이블은 만들지 않는다 — 자리는 한쪽만 보낸 콕도 끌어서(ADR-40),
  라운드를 겹쳐 보여주면 *내가 안 찌른 사람이 자꾸 온다* 가 정확한 목록이 된다.
  방은 기억이 흐릿하지만 목록은 정확하다
- **내 테이블만.** 전체 배치표는 남들끼리의 패턴을 드러낸다. B 는 A–C 를 몰라야 한다 (ADR-24)
- **이유는 없이.** 💘·"커플" 은 운영자 것이고(ADR-51), `서로` 로 시작하는 문장은 없다 (ADR-52)

**순서는 세 층이다** — 발표 후엔 서로 찌른 사람만 위(ADR-18 그대로, 같은 테이블 묶음은 없다 —
매칭 카드가 테이블을 이미 말한다) · 발표 전엔 같은 테이블만 위 · 자리가 없으면
**보는 사람마다 고정된 무작위**. 등록 순서는 *누가 먼저 왔나* 라서 이제 안 쓴다.
무작위의 열쇠가 `해시(나, 상대)` 라 새로고침에 안 바뀌고, 새로 등록한 사람은 끼어들 뿐 남을 밀지 않는다.

`이성만` 을 켜면 묶음도 같이 걸러지고, 동성뿐인 테이블이면 머리글도 사라진다.

### 그밖에

- **2.3.0 노트의 빈 두 자리를 채웠다** — 자리 미확인자 목록(6)과 글자체(7). 나간 뒤에 적혔다
- **테스트 11개**가 위 선 셋을 지킨다. 초안이 새는 길이 하나 늘어서 `초안은 참가자 응답에 없다` 에
  `mateIds` 부재 단언을 더했다

### 아직 정하지 않은 것

**첫 화면 번들이 199.6 / 200.0 KiB 다.** 이번 판이 +0.6 을 더했고 예산은 그대로 뒀다 —
다음에 첫 화면에 무언가를 더하는 사람이 걸린다. 그때 *"이게 첫 화면에 있어야 하는가"* 를 다시 묻는다.
`LLM_TEMPERATURE` 는 2.1.0 에서 적은 그대로 프로덕션에 없다.

---

## 2.3.0 — 링크를 누르면 바로 보인다

이번 판의 큰 줄기는 **속도**다. ADR 넷(69·70·71·72). *"링크를 눌러 등록을 시작하기까지가 느리다"*
는 말에서 시작해, 4G(RTT 150ms)에서 프로덕션 번들로 재보고 고쳤다.
참가자 신고에서 나온 고침(ADR-71)과 화면 손질, 운영자 콘솔의 **자리 미확인자 목록**,
그리고 **글자체**(ADR-72)가 함께 나간다.

| | 전 | 후 |
|---|---|---|
| **첫 그림**(FCP) | 1404ms | **468ms** (−67%) |
| 두 번째 방문 FCP | 356ms | **196ms** (−45%) |
| 링크 → 등록 1스텝 입력 가능 | 1820ms | **1391ms** (−24%) |
| 번들 | 437KB | **372KB** (gzip 139 → 122) |

### 1. 화면이 번들을 기다리지 않는다 (ADR-70)

폰으로 링크를 열면 오로라만 깔린 **빈 화면이 1초 넘게** 있었다. `#root` 가 비어서다 —
HTML 은 157ms, CSS 는 325ms 에 와서 바탕을 칠하는데 **그릴 것이 번들 안에만 있었다.**
데이터는 319ms 에 이미 도착해 있었지만, 그걸 그릴 React 가 1158ms 에야 왔다.

> 사용자는 눈으로 확인하는 게 먼저다. 기능은 늦게 살아나도 되지만,
> **누른 것에 반응한 것처럼 보이는 것**이 가장 중요하다.

- **로고를 번들에서 꺼냈다.** 화면에서 가장 큰 그림인데 `import` 로 묶여 있어
  JS 가 다 온 뒤에야 요청이 나갔다. 고정 주소를 주고 `preload` 로 당긴다 —
  165ms 에 출발해 314ms 에 도착한다
- **모양을 HTML 에 세운다.** `#root` 안의 스켈레톤은 로고와 자리 표시뿐이고
  **데이터도 문구도 없다** — `copy.ts` 와 겹치지 않으니 거짓을 말할 것이 없고,
  React 가 올라오면 통째로 갈린다
- **첫 프레임부터 어둡다.** 그릴 것이 생기니 CSS 이전 구간이 **흰 바탕**이 됐다.
  어두운 앱에서 그 번쩍임은 느린 것보다 나쁘다

**FCP 를 이제 로고가 정한다** — 번들(1158ms)과 무관해진 것이 이 변경의 요점이다.
색 막대는 FCP 를 만들지 않는다(배경색은 contentful 이 아니다). 스켈레톤이 하는 일은
**숫자를 줄이는 게 아니라 빈 화면을 없애는 것**이고, 그 둘은 다른 값이다.

### 2. 묻는 일과 받는 일이 나란히 간다 (ADR-69)

병목은 크기가 아니라 **직렬성**이었다. 회차를 묻는 데 153ms 밖에 안 걸리는데,
그 앞의 1초를 번들 기다리는 데 썼다.

- **회차 조회가 번들보다 먼저 출발한다.** HTML 을 파싱하는 순간(167ms) 나가서
  329ms 에 답이 온다 — React 가 올라올 때는 이미 손에 있다
- **운영자 콘솔을 따로 내려받는다.** 참가자는 운영자 아홉 화면(2791줄)을 **한 번도
  열지 않는데** 함께 받고 있었다. 참가자 앱은 **등록 폼을 채우는 동안** 미리 받는다 —
  세 스텝을 채우는 십수 초면 충분하고, 어차피 망이 노는 시간이다
- **등록 화면이 같은 것을 두 번 묻지 않는다.** `Join` 이 읽은 값을 넘긴다.
  받는 쪽은 **없어도 동작한다** — 넘기는 건 최적화이지 계약이 아니다
- **환경 띠가 크리티컬 패스에서 빠졌다.** 프로덕션에서도 매 로드마다 `/health` 를
  불렀다 — 라벨이 없어 아무것도 안 그리면서도 묻는 일은 했다

남은 372KB 는 사실상 vendor 다. 한글 문구는 번들에 13KB 뿐이고,
`copy.ts` 가 121KB 인 건 대부분 주석이다.

### 3. 번들에서 꺼낸 파일은 캐시 규칙에서도 빠져 있었다 (ADR-70 후기)

1번이 로고를 `public/` 으로 옮기면서 `/assets/*` 영구 캐시 규칙 **밖으로** 나갔다.
정적 자산의 기본값은 `max-age=0, must-revalidate` 라, **두 번째 방문부터 로고가
304 왕복을 기다렸다.** 그 방문에서는 CSS·번들이 전부 캐시라 200ms 안에 다 서는데
**화면에서 유일하게 contentful 한 것이 로고**여서, preload 로 벌어둔 것을 도로 내준 셈이다.

하루는 묻지 않고 쓰고 그 뒤 일주일은 일단 그리고 뒤에서 새로 받는다.
해시가 없으니 `immutable` 은 못 쓴다 — 로고를 바꿨는데 영영 안 바뀌면 그게 더 나쁘다.

### 4. 회차 확인 화면을 손봤다

- 로고와 그 뒤의 빛이 **40px 내려간다.** 두 값을 하나(`--logo-drop`)로 묶어서 따로 놀지 않는다 —
  자리를 정하는 방식이 서로 달라(`margin-top` 과 그라디언트 위치) 따로 두면 한쪽만 고쳐진다
- 로고가 **8% 작아진다.** 후광은 그대로다 — 둘 다 `58%` 지만 **재는 상자가 서로 달라서**
  따라 줄지 않는다
- **스켈레톤에서 진짜 화면으로 넘어갈 때 로고가 튀지 않는다.** 위 둘을 넣으면서
  `index.html` 의 스켈레톤을 안 고쳐 40px + 8% 가 어긋나 있었고, 거기에 **ADR-70 부터 있던
  16px** 이 더해져 있었다 — 스켈레톤은 위 여백이 `0`, `.body` 는 `var(--pad)` 라
  로고 스타일이 글자 하나 안 틀려도 자리가 달랐다. 합쳐서 56px 튀었다.
  1번이 없애려던 바로 그 종류라 같이 고쳤다

### 5. 회차를 잃은 화면이 다른 파티로 데려가지 않는다 (ADR-71)

참가자 신고다. 지워진 회차의 주소를 열면 `그런 회차가 없어요` 가 맞게 떴는데,
**그 아래 버튼을 누르면 참석 중인 다른 회차 안에 들어가 있었다.**
회차를 둘 이상 등록한 사람에게만 나타난다.

- **회차를 잃은 실패 화면에는 버튼을 두지 않는다.** 이 앱에서 회차에 들어가는 길은
  참가 링크 하나뿐이라(ADR-13·15·32) 그 자리에서 앱이 대신 눌러줄 수 있는 것이 **없다.**
  참가 링크가 죽었을 때가 이미 그 모양이었다 — 문장만 있고 버튼이 없다.
  다시 시도하면 달라지는 실패(끊긴 망·서버 탓)에는 버튼을 그대로 둔다
- **회차가 없어진 것과 나만 빠진 것은 다른 문구다.** 회차가 지워졌는데
  *"참가가 삭제되었어요 — 같은 링크로 다시 들어올 수도 있어요"* 라고 말하고 있었다.
  링크도 함께 죽었는데 다시 열라고 하는 셈이라, 참가자는 죽은 링크를 몇 번이고 다시 연다.
  **반대 방향은 이미 규칙이었다** — `지워진 참가자에게 "그런 회차가 없어요" 라고 하지 마라`.
  그 짝이 비어 있었던 것이다

### 6. 자리를 아직 확인 안 한 사람을 이름으로

참가자는 테이블에서 이야기하느라 폰을 잘 안 본다. 자리가 바뀌어도 모른다.
운영자에게는 `자리 이동 확인 12/40명` **숫자뿐**이었다 — 몇 명인지는 알아도 **누구인지**를
몰라 찾아갈 수가 없었다. 화장실에 갔거나 밖에 나간 사람은 방에 대고 말해도 안 들린다.

- **이름과 옮겨갈 테이블을 테이블 순으로 세운다** (`여우 · 3번, 곰 · 3번, …`).
  같은 번호를 받은 사람끼리 붙어 있어야 한 번에 말해줄 수 있다
- **남은 사람이 없으면 사라진다.** 할 일이 없는데 `0명` 을 띄우지 않는다
- **최신 라운드에만, 발표 전까지만.** 지난 라운드는 사람들이 이미 다음 자리에 앉아 있고,
  발표 뒤에는 참가자 화면에 자리 카드가 아예 안 뜬다 — 둘 다 확인이 올 수 없는 자리다
- `안 온 사람` 이 아니라 **`아직 확인 안 한 사람`** 이다. 폰을 안 봤을 뿐인 사람이 대부분이라,
  둘을 같은 말로 부르면 앞에 앉아 있는 사람을 찾으러 나간다. 옆의 번호도 **찾으러 갈 곳이
  아니라 찾으면 알려줄 값**이다 — 아직 안 옮긴 사람이라 지금은 옛 테이블이나 화장실에 있다

**운영자 콘솔 안에서만 바뀐다.** 참가자 화면에는 아무것도 더해지지 않았다.

> 같이 만들었던 **화면을 켜 두는 것(Screen Wake Lock)은 나가기 전에 걷어냈다.** 폰이 위를 보고
> 놓여 있고 앱이 열려 있을 때만 값이 있어서, 정작 폰을 안 보는 사람 — 애초에 풀려던 그 사람 —
> 에게는 닿지 않았다. 꺼진 화면은 웹이 못 켠다. 닿는 통로는 위의 것 하나다.

### 7. 글자체를 앱이 싣는다 (ADR-72)

`ADR-62 ①`(*"웹폰트를 싣지 않는다"*)을 뒤집는다. 기기가 가진 글자체를 쓰던 것을 **SUIT** 로 바꾼다.

- **저울에 안 올렸던 것이 있었다.** 이 앱은 한 테이블에 앉아 **서로 화면을 본다.** iOS 와
  안드로이드가 서로 다른 글자체(SF Pro + Apple SD Gothic Neo 대 Roboto + Noto Sans CJK KR)를
  보고 있었고, 한 문장 안에서 라틴과 한글이 늘 다른 폰트에서 왔다(`여우같은사람 29 ENFP`).
  가장 많이 쓰던 굵기 800 이 기기에 없으면 브라우저가 **가짜 굵기**를 만들어 한글이 뭉개졌다
- **그때의 숫자가 틀렸다.** *"서브셋으로 줄여도 100KB"* 는 3~7배 낙관적이었다. SUIT 는 읽히는
  후보 중 가장 둥글고(로고의 결과 붙는다) 가장 가볍다 — 11,172 음절이 아니라 **2,668자(24%)**
  만 담아서다. 흔치 않은 글자와 장난스러운 닉네임 글자 50개가 다 들어 있는지 재봤다.
  그래도 정말 희귀한 음절은 시스템 폰트로 떨어지므로 스택 뒤의 시스템 폰트는 남는다
- **첫 그림을 막지 않는다.** `font-display: swap` 이고 preload 하지 않는다. 첫 화면은 시스템
  폰트로 즉시 서고 글자체는 뒤따라와 **한 번 바뀐다.** 첫 화면이 이 파일을 기다리면 1·2번이
  한 일이 그대로 없어진다. 두 번째 방문부터는 영구 캐시다 — 파일 이름이 곧 내용이라(`SUIT-Regular.woff2`)
  해시가 붙은 것과 같은 성질이다
- **굵기는 둘뿐이다** (400·700). 한 굵기가 163KB 라 늘리는 값이 비싸다. 500·600·800 을 쓰던
  36곳을 둘로 접었다 — 크기와 색이 이미 층을 만들고 있어 견딘다

**두 굵기 331KB 가 새로 나간다.** 위 표의 번들 숫자에는 들어 있지 않다 — 그래서 `check:bundle` 이
**`첫 화면`(200KB)과 `글자체`(340KB) 두 묶음**으로 갈렸다. 331 을 총합에 얹으면 예산이 530 이
되어 그 숫자가 뜻을 잃는다(이미지 한 장이 여유 안에 조용히 숨는다). 굵기를 하나 더하면 뒤쪽에서 걸린다.
파티장 회선에서 글자가 바뀌는 순간이 얼마나 보이는지는 **실기기에서 아직 안 봤다.**

### 그밖에

- **`check:config` 가 캐시 규칙을 본다.** `index.html` 이 preload 하는 파일에
  `_headers` 규칙이 없으면 잡는다 — 3번이 바로 그 구멍이었다
- **`check:theme` 가 `index.html` 의 늘어난 색을 본다** (ADR-62). 거기는 CSS 변수를
  못 쓰는 자리라 조용히 어긋난다
- **`check:theme` 가 스켈레톤과 `.joinLogo` 를 대조한다.** 4번의 마지막 항목이 그 구멍이었다 —
  "다르면 튄다" 고 주석에 적어 뒀는데 세 번 연달아 깨졌다. 사람이 지킬 규칙이 아니었다

### 아직 정하지 않은 것

**`LLM_TEMPERATURE` 는 여전히 프로덕션에 없다.** 값이 없으면 파라미터를 안 보내
제공자 기본값으로 돈다. QA 에 1.5 가 올라가 있고, 재보고 정한다 — 2.1.0 에서 적은 그대로다.

---

## 2.2.0 — 링크가 카드가 되고, 문 앞에 로고가 선다

2.1.0 이 안쪽 화면들의 결을 맞췄다면 이번엔 **바깥쪽**이다 — 카톡에 링크를 붙인 순간부터
회차 확인 화면의 버튼을 누르기까지, **참가자가 이 앱을 처음 만나는 자리**를 손봤다. ADR 둘(67·68).

### 1. 링크가 카드로 뜬다 (ADR-68)

운영자는 초대 링크를 **1:1 로** 보낸다 (ADR-32). 그 링크가 카톡에서 맨 주소로 떴다 —
받은 사람이 처음 보는 것이 `…/j/<회차id>/<토큰>` 이었다.

- **`og:` 태그를 워커가 끼워 넣는다.** `/j/*` 한 자리에서만, 요청에서 꺼낸 주소로.
  카톡 크롤러는 JS 를 돌리지 않아 정적 `index.html` 로는 안 되고, `og:image` 는
  **절대 주소**여야 한다 — 그 둘이 만나는 곳이 워커다
- **카드에 토큰이 없다.** 주소에서 origin 만 꺼내 쓴다 — 경로도 질의문자열도 거기서 버려진다.
  미리보기가 열쇠를 실어 나르면 그게 곧 명단 밖으로 나가는 길이다.
  주소를 만드는 함수 하나만 밖으로 내보내서, 토큰이 닿을 자리가 아예 없다
- **탭 이름과 아이콘**도 이번에 붙었다. 홈 화면에 추가하면 크림 바탕의 발바닥이 선다

### 2. 회차 확인 화면이 로고 배너가 된다

카드를 누른 사람이 두 번째로 보는 화면이다. 전에는 헤더와 카드가 있었고, 로고는 없었다.

- **로고 · 단계 · 회차 이름 · 일시 · 버튼 하나.** 입력 칸은 여전히 없다 —
  링크의 토큰이 곧 신원이라 **참가자는 전화번호를 치지 않는다** (ADR-32)
- **단계 색은 상단 바와 같은 값을 읽는다**(`--phase`). 두 곳에 베껴 두면 한쪽만 고쳐져
  같은 단계가 화면마다 다른 색이 된다
- **`--cream` 을 로고에서 뽑았다.** 로고 바로 옆 글자를 중성 회색으로 두면 그 자리만
  온도가 어긋난다. 흐리게 쓸 때는 바탕 쪽으로 섞는다 — 검정으로 섞으면 자두빛이 빠져 죽는다

### 3. 바탕은 단계를 말하지 않는다 (ADR-67)

**2.1.0 이 넣은 것을 걷었다.** 등록·사전 투표·파티·발표의 바탕 공기가 서로 다르던 그것이다.

되돌려 달라는 요청에서 시작했는데, 다시 보니 취향 밖에 이유가 하나 더 있었다.
파티장에서 **테이블 건너편은 글자를 못 읽어도 바탕색은 읽는다.** 바탕이 단계를 말하면
그게 곧 *저 사람이 지금 어디쯤인가* 다 — `남이 일으킨 변화에 움직임을 넣지 마라`(ADR-64) 와
같은 누출이고, ADR-61 을 쓸 때 그 규칙은 아직 없었다. 옅게 해도 단계를 말하는 건 그대로다.

**바탕에 주 색을 태운 것은 남는다** — 그 값은 시간에 따라 변하지 않아 아무것도 새지 않고,
걷으면 대비가 오히려 내려간다. 단계는 이제 **상태바의 작은 글자**로만 남는다.
정지한 작은 글자는 가까이서만 읽힌다.

### 4. 회차 이름을 누르면 홈으로

상단 바의 회차 이름 칸이 홈으로 가는 지름길이 된다. 어느 탭에 있든 늘 같은 자리라
하단 홈 탭까지 손을 내리지 않아도 된다. **홈 탭에서는 버튼이 아니다** —
갈 곳이 없는데 버튼으로 서 있으면 눌러본 사람이 고장으로 읽는다.
보이는 것은 아무것도 더하지 않았다. 표시를 붙이는 순간 이 줄이 답하는 질문이 둘이 된다.

### 그밖에

- **번들 예산이 그림도 센다.** 전에는 `.js`·`.css` 만 셌다 — 42 KiB 짜리 로고가 그냥 지나갔고,
  `release-guard` 가 잡으라고 세워둔 것이 그 자리에서 아무 말도 하지 않았다.
  크롤러만 받아가는 것(`og.jpg`·`apple-touch-icon.png`)은 내려받지 않으므로 예산 밖이다
- **브랜드 자산은 스크립트가 만든다**(`scripts/make-brand-assets.mjs`) — 원본 하나에서 넷을.
  원본은 번들 밖(`design/`)에 둔다

### 아직 정하지 않은 것

**`LLM_TEMPERATURE` 는 여전히 프로덕션에 없다.** QA 에 1.5 가 올라가 있고, 재보고 정한다 —
2.1.0 에서 적은 그대로다.

---

## 2.1.0 — 운세가 사람마다 갈리고, 화면이 조용해진다

v2.0.0 이 나간 뒤 **참가자·운영자 피드백에서 나온 것들**이다. ADR 7개(60~66).
눈에 가장 크게 보이는 것은 **운세**와 **화면의 결**이다.

### 1. 운세가 사람마다 갈린다 (ADR-60)

*"글이 너무 길고, 사람마다 크게 다르지 않아 특색이 없다."*

원인은 모델이 아니라 프롬프트였다. **발행되는 운세는 일부러 모호하게 쓴다** — 그래야
아무에게나 들어맞는다(Barnum 효과). 혼자 보는 운세라면 그게 기술인데, 이 앱은
**마흔 명이 한 자리에서 화면을 나란히 본다.** 모호한 글끼리는 서로 구별되지 않는다.

- **여지 표현은 글 전체에 한 번까지.** `~일 수 있어요` 를 권하던 자리다 —
  실제 화면 450자에 여섯 번 나왔고, 서로 다른 주장이 전부 같은 무주장으로 평평해졌다.
  단정 금지는 **앞날**에만 남긴다. *오늘 당신은 듣는 쪽이 편합니다* 는 예언이 아니라 묘사다
- **갈라지는 축을 코드가 준다.** 열넷 중 하나를 씨앗이 고른다 — 같은 사람에게는 늘 같은 결이다.
  프롬프트로 "다양하게 쓰세요" 라고 부탁하는 것과 다르다: **세어볼 수 있다**
- **세 문단, 문단당 2~3문장.** 길이와 특색은 같은 문제다 — 다섯 문단 동안 조금씩 말하면
  개인차가 묽어진다. 무엇을 버렸는지가 곧 그 사람의 결이다
- **별자리·띠는 한 문단이 아니라 한 구절.** *사수자리의 활기와 닭띠의 섬세한 감각* 같은
  판박이가 나오던 자리다
- **미션이 본인 매력을 쓴다.** 다만 자랑이 아니라 **자리 열기**로 —
  *"요리 잘한다고 말해보세요"* 가 아니라 *"음식이 새로 나왔을 때 뭐가 들어간 것 같은지 물어보세요"*.
  어필에는 실패가 있고 실패는 티가 난다 (ADR-1)
- **미션에 운세 본문을 주지 않는다.** 주면 첫 문단을 유의어로 바꿔 옮겨 적었다

### 2. 화면의 결이 달라졌다 (ADR-61·62·64)

- **바탕에 색을 태우고 단계마다 공기를 바꾼다** — 등록·사전 투표·파티·발표가 서로 다른 온도다
- **폰트는 기기가 가진 것을 쓴다.** 웹폰트를 받지 않는다 — 파티장 통신이 느린 자리라
  글자가 늦게 오면 화면이 두 번 그려진다. 한글 조판(줄 간격·자간)만 앱이 쥔다
- **시트는 아래에서 오고 아래로 간다.** 방향이 곧 정체다 — 그래서 **탭 전환은 움직이지 않는다.**
  둘 다 움직이면 그 구분이 사라진다. 남이 일으킨 변화는 조용히 온다

### 3. 초대 명단 시트가 가려지지 않는다 (ADR-63·65)

운영자가 명단을 보려고 열면 **키보드가 화면 절반을 먹고, 알림이 명단을 덮었다.**

- **열었다고 입력칸에 커서를 주지 않는다.** 원인은 우리 코드가 아니라 대화상자 라이브러리의
  기본 동작이었다 — 시트 다섯 중 텍스트 입력을 가진 게 여기 하나뿐이라 여기서만 났다
- **화면이 이미 말한 것을 토스트가 또 말하지 않는다.** 더하면 행이 생기고 숫자가 오른다 —
  덮어가며 다시 말할 것이 없다. **복사만 남긴다** — 클립보드는 눈에 안 보인다.
  다만 아래에 안 띄우고 **누른 버튼이 잠깐 바뀐다**
- 순서를 **자주 하는 일부터**로 세운다 — 안내문 → 더하기 → 명단
- 잠긴 뒤에는 내 정보 탭에서 **고치는 자리가 통째로 빈다**

### 4. 기계가 더 본다 (ADR-66)

- **CI 가 두 차선이다.** 잡을 쪼개지 않고 **프로덕션 길목에만** 하나 더 둔다(`release-guard`) —
  옛 스키마와 번들 예산을 거기서 본다. 이 저장소가 실제로 다친 곳이 러너라, 잡을 늘리면
  기다리는 시간이 는다
- **ADR 목차가 어긋나면 기계가 잡는다**(`check:adr`). 브랜치가 여럿 돌면서 번호가 자꾸 겹쳤다
- **테마 검사**(`check:theme`) — 색·인라인 크기·폰트 스택
- **운세 측정 도구**(`scripts/fortune-spread.mjs`) — 마흔 명분을 뽑아 서로 얼마나 닮았는지 잰다

### 아직 정하지 않은 것

**`LLM_TEMPERATURE` 는 프로덕션에 없다.** 값이 없으면 파라미터 자체를 안 보내
제공자 기본값으로 돈다. QA 에서 재보고 정한 뒤에 옮긴다 — 재봐야 아는 값이다.

---

## 2.0.0 — 링크로 들어오고, 두 번 고르고, 연락처는 나가지 않는다

v1 이 한 바퀴 돈 뒤 **파티를 세 번 치르며 나온 것들**이다. ADR 28개(32~59)가 붙었고,
그중 **여섯 개가 v1 의 결정을 되돌린다** — 아래 `되돌린 것` 을 먼저 읽는 편이 빠르다.

### 1. 입장 방식이 바뀌었다 — 전화번호에서 개인 링크로

**참가자는 이제 아무것도 치지 않는다.** 명단 한 줄마다 토큰이 생기고,
`/j/<회차id>/<토큰>` 이 곧 신원이다 (ADR-32).

- 번호는 **열쇠가 못 됐다.** 같은 파티에 오는 사람들은 서로 번호를 아는 사이라,
  번호를 아는 사람이 그 사람이 될 수 있었다 (ADR-15 후기 2)
- 등록 폼에서도 수정 폼에서도 **번호를 받지 않는다.** 서버가 토큰에서 꺼내 쿠키에 담는다 —
  폼에서 받으면 명단에 없는 번호로 바꿔 낼 수 있다 (ADR-31)
- 입장 실패 문구는 **하나뿐**이다. "초대되지 않았어요" 와 "그런 회차가 없어요" 를 가르면
  그 구분이 곧 *이 사람이 이 파티에 있나* 의 답이 된다
- 링크를 잃으면 운영자가 안내문을 다시 보낸다. **재발급 화면은 만들지 않았다**

**남는 구멍은 "링크를 본 사람"이다.** 친구가 폰에서 보면 그 사람이 될 수 있다 —
알고 고른 대가다. 사고가 나면 비밀번호를 연다.

**세션은 탭마다 갈린다** (ADR-44). 쿠키는 브라우저 단위라, 링크가 사람마다 달라도
세션이 하나면 두 번째 탭이 첫 번째 탭을 덮었다. 이제 쿠키를 사람마다 따로 둔다
(`tp_play_<이름표>` · `x-tp-ref` · 소켓 `?ref=`).

### 2. 고르는 일이 둘로 갈렸다 — 매력 투표와 콕 찌르기

한 라운드였던 것이 **성격이 다른 둘**이 됐다 (ADR-34).

| | 매력 투표 | 콕 찌르기 |
|---|---|---|
| 언제 | 만나기 **전**, 며칠에 걸쳐 | 파티에서 만나본 **뒤** |
| 무엇을 보고 | 프로필 (나이·MBTI 는 아직 닫혀 있다) | 직접 만난 사람 |
| 쓰이는 곳 | **첫 자리 배정** | 다음 자리 배정 + **발표** |
| 마감 | 파티 직전, **시각으로** (ADR-39) | 발표까지 |

- **매칭은 파티 콕만 센다.** 프로필만 보고 서로 골랐다는 건 만나본 뒤의 결과를
  더 설명해 주지 않는다 — `MatchKind` 를 통째로 걷어냈다
- **되돌리기·알림은 라운드마다 따로 정한다** (`allowUndoPre`·`allowUndo` ·
  `preNotify`·`pokeNotify`). 회차 설정에서 고른다
- 알림을 **끈 라운드는 받은 수 자체가 안 내려간다** (ADR-43, `visibleReceived`) —
  화면에서 감추는 것만으로는 개발자 도구가 연다
- 자리의 끌림은 **표 하나하나가 만든다** (ADR-40). 예전에는 상호냐 단방향이냐 두 칸뿐이라
  세 번 투표한 것과 한 번 투표한 것이 자리에서 똑같았다
- 받은 콕은 **한 번에 하나씩** 쌓이고 **시각이 붙지 않는다.** 소식 칸의 차례는
  라운드로만 준다 (ADR-48) — 시각을 주면 그때 누가 있었는지와 맞춰 발신자가 좁혀진다

### 3. 연락처가 참가자에게 나가지 않는다

v1 에서 가장 크게 되돌린 자리다.

- 발표 후 서로 찌른 쌍에게 나가는 건 **실명 하나**다 (ADR-42, `MatchInfo.realName`)
- `내 정보` 탭의 번호·인스타 줄도 없앴다 (ADR-47). 번호는 참가자 응답에 **아예 없고**,
  인스타는 고치는 칸에만 있다
- 인스타를 받는 이유는 **운영자 확인 하나**다. 등록 화면이 그렇게 말한다
- 도움말의 `그때 연락처가 열려요` 는 **거짓이었다** (ADR-52) — 지웠다.
  대신 없다는 것도 답이라 적는다: `연락처는 앱이 전하지 않아요 — 그 자리에서 직접 나누세요`
- 도움말이 **회차 설정을 따라간다** (ADR-52) — 되돌리기·알림이 회차마다 다른데
  한 가지로만 말하면 그 회차에서는 도움말이 틀린 것이다
- 발표 안내는 **매칭이 있는 사람에게만** 참가자 탭을 가리킨다 (ADR-53).
  없는 사람에게는 그 길이 틀렸다 — 가 봐야 💘 없는 명단뿐이고, 답은 이미 홈 카드에 있다

### 4. 자리 배정이 한 버튼이 됐다

- `💘 커플 자리 배정` 을 걷어냈다 (ADR-51). 콕이 매 라운드 자리에 들어가므로
  쌍만 모으는 전용 라운드가 필요 없다
- 대신 자리 검토 화면이 **쌍을 짚어준다** — `💘` 같은 테이블 · `💔` 다른 테이블(옮길 수 있다).
  못 붙은 쌍은 운영자가 맞교환으로 옮긴다
- 배정 전에 **뺄 사람을 고른다** (ADR-45). 이 라운드에만 빠지고 어디에도 저장되지 않는다
- **발행된 자리는 잠겨 있다** (ADR-49). 최신 라운드만, `수정하기` 를 눌러야 열린다
- 자리는 **발행하면 그때부터 보인다** (ADR-39) — 단계로 막지 않는다.
  짜는 동안 새지 않게 하는 일은 초안이 맡는다

**짜는 방식도 바뀌었다** (ADR-57). 마지막 라운드를 특별하게 다루던 구조를 걷고,
**개인 만족의 합**으로 짠다 — 운영자는 시계를 보고 파티를 끝내므로 *이번이 마지막인가* 에
답할 수가 없다. 대신 상호 콕이 갚은 뒤에도 계속 끌리게 두면, 새로 만날 사람이 고갈되며
**후반이 저절로 콕 라운드가 된다** (실측 마지막 라운드 동석 97%).
확률적 규칙이라 씨앗 하나로 재지 않고 **여러 개를 돌려 평균으로** 본다.

### 5. 일정과 잠금

- **등록은 회차를 만드는 순간 열린다** (ADR-38). 등록 시작 시각을 받지 않는다
- **커플 발표에 예약이 생겼다** (ADR-43). 다만 **파티가 시작된 뒤에만 울린다** —
  아무도 안 온 자리에서 발표가 뜨면 매칭 0으로 끝난 것이 된다
- **매력 투표 마감은 예약이 아니라 판정이다** (ADR-39). 단계는 `prevote` 그대로다
- **마감되면 참가자에게 소식이 간다** (ADR-55). 전에는 콕 버튼이 조용히 잠기기만 해서,
  그 순간 앱을 보고 있지 않았던 사람에게는 아무 일도 없었던 것이 됐다.
  예약대로 닫히는 마감은 **아무도 밀어주지 않아서**(그 순간 코드를 돌리는 사람이 없다)
  참가자 화면이 마감이 가까우면 스스로 다시 그린다
- **파티 시작은 예약 스텝에서 빠졌다** (ADR-54). 회차 만들기 1스텝(기본 정보)으로 옮겨
  이름·장소와 함께 선다 — 나머지 일정이 거기서 거꾸로 계산되는 기준점이라 가장 먼저 정한다.
  덕분에 **2스텝에 남은 것은 전부 저절로 넘어가고**, 예외가 없으니 예외를 설명하던 줄도 걷었다.
  ("이 시각은 예약되지 않아요" 로 구조를 문장이 대신 떠받치고 있었다)
- **운영자 화면의 곁설명을 걷었다** (ADR-54 후기 2). 위저드 2스텝의 설명 넷과
  설정 탭 `콕 설정` 의 곁설명 셋(`pokeTargetNote`·`preNotifyNote`·`pokeNotifyNote`)이 사라졌다 —
  **라벨만으로 알 수 있는 칸에는 설명을 붙이지 않는다.** 토글 다섯에 설명 셋이 붙으니
  켜고 끄는 자리가 읽는 자리가 됐다. 고른 결과가 라벨에 안 드러나는 곳(3스텝 토글·장소)은 남겼다
- **규칙과 일정은 콕이 오가기 시작하면 굳는다** (ADR-35). 콕 횟수만 일부러 열려 있다 —
  매칭이 모자랄 때의 손잡이다
- **발표는 되돌릴 수 없다** (ADR-50)
- **닉네임 칸 안내 문구를 운영자가 적는다** (ADR-59). 회차마다 바뀌는 값이 아니라
  그 운영자가 여는 파티의 성격에 붙는 값이라 **기본값 화면**에 두고, 회차 설정에서 덮어쓴다.
  **앱이 얹는 고정 줄은 없고 기본값도 비어 있다** — 우리가 채운 문장이 붙으면
  운영자가 자기가 고른 건지 구분하지 못한다

### 6. 사용성·성능·자리 배정을 잰다 — **집계로만** (ADR-56·58)

파티 회차·앱 사용성·성능에 답할 자료가 없었다. 이제 잰다:
**API 응답 시간** · **화면 진입**(등록 이탈·탭 쓰임새) · **버튼** · **소켓 끊김**(파티장 와이파이) ·
**체류 시간 버킷**.

**개인 단위로는 쌓지 않는다.** 회차 DO 밖으로 나가는 것에 사람도 회차도 상대도 없다 —
`pulse()` 는 `eventId` 를 받을 자리가 아예 없고, 담기는 값은 허용 목록에서만 고른다.
인원이 적으면 `회차 X · 등록 2단계 이탈 1건` 이 그 한 사람이고, 지표는 **파기 뒤에도 남는다.**
`Web Analytics` 는 붙이지 않았다 — URL 로 집계해서 참가 토큰이 그대로 나간다.

**자리 배정은 운영자가 손댄 횟수로 잰다** (ADR-58). 사람이 고쳤다는 건 계산이 놓쳤다는 뜻이다 —
초안을 몇 번 다시 뽑았나 · 한 배정에 몇 번 맞교환했나 · 자동 배정을 아예 못 믿고 섞었나.
**결과 통계는 넣지 않는다.** 가장 알고 싶은 값(마지막 라운드 상호 콕 동석률)이 정확히 그 선 밖이다 —
상호 쌍이 두셋뿐인 회차에서 `3쌍 중 2쌍 동석` 은 **누가 서로 찔렀는지**를 좁히고,
회차를 지운 뒤에도 90일 남는다.

### 7. 자동 파기를 걷어냈다

`파티가 끝나고 N일 뒤에 지워져요` 라고 적혀 있었지만 **지우는 코드가 없었다**. (ADR-36)
문구를 지웠다. 회차는 운영자가 `이 회차 삭제하기` 를 누를 때까지 남는다.

### 되돌린 것

| 되돌린 것 | 무엇을 | 왜 |
|---|---|---|
| ADR-36 | ADR-19 의 보관 기간 | 지우는 코드가 없는데 문구가 있었다 |
| ADR-42 | ADR-19 · ADR-37 | 연락처는 매칭된 쌍에게도 안 나간다 |
| ADR-45 | ADR-33 · ADR-41 | 참석 상태는 현장에서 사람이 센다 |
| ADR-46 | 왕관 카드 | 순위를 라운드마다 따로 센다 |
| ADR-51 | ADR-23 · ADR-28 | 커플 자리 라운드 자체가 없어졌다 |
| ADR-52 | ADR-23 의 자리 함구 (절반) | `내가 고른 사람과` 까지는 말한다 |

### 참가자 응답에서 빠진 것

개발자 도구로 열어도 없다.

`phone` · `instagram` · `contact` · `realName`(매칭 상대 제외) · `fromId`(발표 전) ·
`MatchKind` · `MySeat.final` · `oneWay`(운영자 응답에도 없다)

### 깨지는 변경

**v1 과 호환되지 않는다.** 참가자 응답의 모양이 바뀌었고(`me` 가 `MyProfile` 이다),
`SeatingInput.final`·`SeatingRound.final`·`MySeat.final` 이 없어졌으며,
회차 설정에 키 넷이 늘었다. v1 회차 데이터는 **v1 에서 끝낸 뒤** 올린다.

**DB 는 그대로 열린다.** 안 쓰는 칸 셋(`players.attendance`·`players.contact_share`·`seatings.final`)을
`CREATE TABLE` 에서 뺐지만 `DROP COLUMN` 은 하지 않는다 — **새 회차에 안 생길 뿐, 옛 표는 건드리지 않는다.**
읽는 코드가 없어서 있든 없든 같고, `INSERT` 가 칸을 나열하므로 양쪽 표에서 똑같이 동작한다.


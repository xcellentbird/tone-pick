node_modules/
dist/
.wrangler/
.dev.vars
.env
.env.*
!.env.example
*.local
.DS_Store
npm-debug.log*

# Cowork 원격 세션이 지우지 못한 파일을 모아두는 곳 (사용자가 직접 삭제)
_to_delete/

# 회고용 뽑기. **누가 누구를 일방적으로 좋아했는지가 여기 들어온다** —
# 저장소에 들어가면 GitHub 로 나간다. 이 줄은 커밋해 두는 게 맞다
tmp/

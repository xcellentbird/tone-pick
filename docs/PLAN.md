# tone-pick 작업 계획

최종 갱신 2026-08-11

프로토타입(`reference/poke-party.html`)에서 기획·문구·엣지 케이스가 확정됐다.
이 문서는 그걸 **서버 구조로 옮기는 순서와 방법**을 정한다.

---

## 1. 지금 어디에 있나

| 항목 | 상태 |
|---|---|
| 기획·UI/UX·엣지 케이스 | ✅ 확정 (Claude 프로젝트 문서 10종) |
| 스택 결정 | ✅ Cloudflare Workers + DO(SQLite) + Vite/React, 월 0원 |
| 규칙 문서 | ✅ `CLAUDE.md` |
| 문구 상수 + 검사 | ✅ `src/shared/copy.ts` + `npm run check:copy` |
| **서버** | ✅ 인증·회차·등록·콕·자리·발표. 상태 변경은 전부 DO 안 |
| **화면** | ✅ 참가자 4탭 · 운영자 4탭 · 위저드 |
| **테스트** | ✅ 56개 (규칙 47 + 화면 9) |
| 배포 리허설 | ⬜ 슬라이스 08 — 실제 인원 부하·개인정보 파기 Cron |

---

## 2. 작업 방식

한 슬라이스를 5단계로 돈다. **테스트를 쓰는 쪽과 통과시키는 쪽을 분리**하는 게 핵심이다.
그래야 "테스트를 고쳐서 통과시키는" 부정행위가 구조적으로 막힌다.

| 단계 | 누가 | 산출물 | 규칙 |
|---|---|---|---|
| 1 시나리오 | 사람 + Claude | `docs/scenarios/NN-*.md` (Given-When-Then) | 여기 없는 건 영원히 안 만들어진다 |
| 2 공개 표면 | Claude | `docs/scenarios/NN-surface.md` | 타입·라우트까지만. **클래스·인터페이스 만들지 않음** |
| 3 테스트 | Claude | `test/NN-*.test.ts` | 공개 표면에만 붙인다. 내부 구조를 알면 안 된다 |
| 4 구현 | Codex | 코드 | 내부 구조는 자유 |
| 5 리뷰 | Claude | 수정 + 기록 | **빠뜨리지 않는다** |

### 왜 5단계가 필수인가

테스트가 통과해도 안 지켜지는 게 있다. 특히 **문구**. 모든 테스트를 통과시키면서
확인창에 "정말 삭제하시겠습니까?"를 쓰는 건 완벽히 가능하다.

리뷰에서 보는 것:

1. 문구를 `copy.ts` 에서 가져왔는가 (`npm run check:copy` 가 1차로 잡는다)
2. 확인 다이얼로그가 무엇이 바뀌는지 숫자로 보여주는가
3. `docs/DOMAIN.md` 의 설계 결정을 "개선" 하지 않았는가 — 특히 **일회성 알람 모델**
4. 구현이 하나뿐인데 인터페이스·팩토리를 만들지 않았는가
5. 슬라이스 밖의 기능을 미리 만들지 않았는가

### 매 슬라이스 시작 전 확인

**구현 전에 통과하는 테스트가 있는지 본다.** 있으면 그 테스트는 가짜다.

슬라이스 01 에서 실제로 두 개 나왔다.
`S-C2`(보안 테스트)는 501 응답에 본문이 없어서 "비밀이 없다"가 저절로 참이 됐고,
`S-B2b`는 생성 실패 시 `continue` 해서 0번 검증했다. 둘 다 **Codex 가 아무것도 안 해도 통과**했을 테스트다.

### 이 환경의 제약

| 작업 | 누가 |
|---|---|
| 코드 편집, 타입체크·빌드·테스트 | Claude (클라우드 컨테이너) |
| `git add` / `commit` | 사람 — Claude 통로는 잠금 파일을 못 지운다 |
| `git push`, `npm install`, `wrangler deploy` | 사람 — Claude 통로에 네트워크가 없다 |

---

## 3. 슬라이스 로드맵

| # | 슬라이스 | 범위 | 의존 | 위험 |
|---|---|---|---|---|
| **01** | 회차 생성 + 입장 코드 | 인증, 위저드, 코드 조회, 단계 자동 전환 | — | PIN 권한 경계 |
| 02 | 참가자 등록 | 3스텝, 닉네임 유일성, 재접속, 매력 3가지 | 01 | 동시 등록 경합 |
| 03 | 콕 찌르기 | 예산 분리, 중복·0회, 확인, 익명 알림 | 02 | **응답 경계(발신자 노출)** |
| 04 | 단계 운영 + 실시간 | 수동/예약 공존, 브로드캐스트, 알림 파생 | 03 | 예약 알람 모델 훼손 |
| 05 | 자리 배정 | `buildSeating` 이식, 발송·확인, 마지막 자리 | 03 | **무료 플랜 CPU 10ms** |
| 06 | 발표 + 공개 범위 | 상호 매칭만 공개, 되돌리기 | 03, 05 | **개인정보 유출** |
| 07 | 화면 마감 | 라우팅·뒤로 가기, 모바일 | 전부 | iOS 100dvh·스와이프 |
| 08 | 배포 + 리허설 | 시크릿, 100명 부하, 개인정보 파기 Cron | 전부 | 실기기 미점검 |
| 09 | 내 정보 고치기 | 사전 투표 전까지 열림 (ADR-31) | 02 | — |
| 10 | 참가자 탭의 내 카드 | 남들이 보는 모습 미리보기 | 02 | `toPublic()` 재구현 유혹 |
| 11 | 앉힌 자리 고치기 | 맞교환·앉히기·자리 비우기 | 05 | 단일 이동 API 유혹 |
| 12 | 자리 보이는 때 | 파티 시작부터. 시작 버튼이 곧 알림 | 05 | — |
| **13** | **개인별 참가 링크** | 초대마다 고유 토큰. **번호를 묻지 않는다** | 02 | 운영자 1:1 발송 동선 |

**순서를 이렇게 잡은 이유** — 03(콕)이 데이터의 중심이라 05·06이 전부 여기에 매달린다.
07(화면)을 뒤로 미루는 대신, 라우팅 구조는 이미 골격에 넣어뒀다. 나중에 붙이면 전면 수정이라서.

### 13 이 다음 순서인 이유

지금 **번호를 아는 사람은 그 사람의 계정을 통째로 가져간다** (ADR-15 후기 2).
발표 후에는 그 사람과 서로 찌른 상대의 연락처까지 따라간다 — 이 앱에서 가장 비싼 유출이다.

개인별 링크는 그걸 닫으면서 **참가자가 할 일도 줄인다** — 번호를 치지 않고 링크만 연다.
대신 운영자가 1:1 로 보내야 하므로, 설계에서 풀 것은 **발송 동선**이다
(명단 화면에서 사람마다 링크를 복사하거나, 문자·카톡으로 넘기기 좋은 모양).

SMS 인증은 유일하게 완전히 닫지만 이 규모에는 과하다고 봤다. 그 판단의 근거는 ADR-15 후기 2 에 있다.

**05 는 시작 전에 CPU 시간을 측정한다.** 넘치면 iteration 을 줄이거나 유료 $5/월로 간다.

---

## 4. 파일럿에서 잴 것

슬라이스 01 은 기능이 아니라 **작업 방식**을 검증하는 게 목적이다.

| 지표 | 어떻게 재나 | 판단 |
|---|---|---|
| Codex 왕복 횟수 | 테스트 전부 통과까지 몇 번 주고받았나 | 3회 이하면 좋음 |
| 리뷰 지적 건수 | 5단계에서 잡은 것 | **3건 이하면 워크플로 유지**, 넘으면 조정 |
| 테스트가 놓친 것 | 리뷰에서만 잡힌 항목 | 다음 슬라이스에서 **테스트로 승격** |
| 가짜 테스트 | 구현 전 통과한 개수 | 0 이 목표. 01 은 2건 나왔다 |
| 체감 속도 | Claude 가 직접 짰을 때 대비 | 느리면 역할을 바꾼다 |

**조정 후보** — 리뷰 지적이 많으면 (a) Codex 프롬프트에 규칙을 더 싣거나
(b) Codex 를 구현자가 아니라 **적대적 리뷰어**로 돌린다.
같은 모델이 자기 코드를 검토하면 맹점을 공유하므로, 후자가 더 값어치 있을 수 있다.

---

## 5. Codex 에 넘길 때

`CLAUDE.md` 를 Codex 가 자동으로 읽지 않는다. 프롬프트에 직접 싣는다.

```
저장소: tone-pick. 슬라이스 01 을 구현해줘.

먼저 읽을 것:
  CLAUDE.md
  docs/scenarios/01-event-create-join.md   (시나리오)
  docs/scenarios/01-surface.md             (계약 — 여기 적힌 것만 계약이다)

목표: npm test 의 22개를 전부 통과시킨다.

지켜야 할 것:
  · 테스트 파일을 수정하지 마라. 규칙이 틀렸다고 생각되면 고치지 말고 말해달라
  · 내부 구조는 자유다. 다만 인터페이스는 구현이 둘 이상일 때만 만든다
  · 사용자에게 보이는 문구는 src/shared/copy.ts 에서 가져온다. 새로 짓지 마라
  · 상태를 바꾸는 건 EventDO 안에서만. Worker 는 인증과 라우팅만 한다
  · 예약은 한 번만 울리는 알람이다. 일반 스케줄러로 "개선" 하지 마라

완료 조건: npm run check 와 npm test 가 모두 통과
```

---

## 6. 리스크

| 리스크 | 지금 상태 | 대응 |
|---|---|---|
| 무료 플랜 CPU 10ms | **통과** (자리 배정 261ms, 100명) | 넘치면 $5/월 |
| 100명 동시 접속 | **검증** — 50명에서 어느 구간도 줄을 안 선다 | 2026-08-20 재측정. 그 전 숫자는 도구가 만든 것이었다 |
| 운세·미션 구간 부하 | **검증** — 운세 p50 5.4초(=LLM 시간) | 서버는 관여하지 않는다 |
| 개인정보 유출 | 타입·테스트로 1차 방어 | 06 에서 전용 테스트. `toPublic()` 단일 경로 유지 |
| **번호를 아는 사람의 사칭** | **열려 있음** — 번호가 곧 그 사람의 계정이다 | 다음 업데이트에서 **개인별 링크** (ADR-15 후기 2). 그때까지는 발표를 현장에서 누르는 운영이 완화책 |
| 워크플로가 느릴 수 있음 | 미검증 | 파일럿 지표로 판단, 안 맞으면 역할 교체 |
| Supabase 미사용 결정 | 확정 | 재검토 안 함 (무활동 7일 정지 + 상업 제한) |

---

## 8. 운영 지표

`npm run ops` — 최근 24시간. `npm run ops -- qa 72` 로 환경·기간을 바꾼다.

**무엇을 세는가**

| | 왜 이건 추측이 아닌가 |
|---|---|
| **입장 시도 결과** (통과·명단에 없음·시도 초과·번호 형식) | iPhone 연락처의 `+82` 번호가 명단에 잘못 들어간 걸 겪었다. 이 숫자가 있었으면 보였다 |
| **운세 규칙문구 비율** | LLM 이 조용히 죽어도 화면은 멀쩡히 뜬다 (ADR-20). 지금은 워커 로그에만 남고 아무도 안 읽는다 |

**경계** — 회차 DO 밖에 쌓이는 유일한 자료다. 규칙은 `src/server/metrics.ts` 주석에 있고,
한 줄로 줄이면 **운영 카운터는 넣고 결과 통계는 넣지 않는다.**
두 명짜리 회차에서 `매칭 1쌍` 은 *그 둘이 서로 찔렀다* 는 뜻이고, 그건 파기 뒤에도 남는다.

**한계**
- 조회에 걸리기까지 **몇 분** 든다. 파티 중 실시간 감시는 `wrangler tail` 이 맡는다
- `_sample_interval` 을 곱해야 실제 건수다. 그냥 세면 조용히 적게 나온다
- **문턱을 코드가 정하지 않는다.** 기계가 "정상" 을 정하면 사람이 숫자를 안 보게 된다

**아직 안 만든 것** — 등록 이탈률, 자리 이동 확인율. 파티를 한 번 치르고 나서
*무엇이 궁금했는지* 를 알고 만드는 편이 낫다. 지금 상상해서 만든 지표는 대개 안 본다.

## 7. 진행 현황

| 슬라이스 | 1 시나리오 | 2 표면 | 3 테스트 | 4 구현 | 5 리뷰 |
|---|---|---|---|---|---|
| 01 회차 생성 + 입장 | ✅ 26개 | ✅ | ✅ 22개 | ✅ | ✅ |
| 02 참가자 등록 | ⬜ 문서 없음 | ⬜ | ✅ 13개 (03·06과 함께) | ✅ | ✅ |
| 03 콕 찌르기 | ⬜ 문서 없음 | ⬜ | ✅ | ✅ | ✅ |
| 04 단계 운영 + 실시간 | ⬜ 문서 없음 | ⬜ | ✅ (되돌리기 포함) | ✅ | ✅ |
| 05 자리 배정 | ⬜ 문서 없음 | ⬜ | ✅ 12개 | ✅ | ✅ |
| 06 발표 + 공개 범위 | ⬜ 문서 없음 | ⬜ | ✅ | ✅ | ✅ |
| 07 화면 마감 | ⬜ 문서 없음 | ⬜ | ✅ 9개 | ✅ | ⬜ 실기기 미점검 |
| 08 배포 + 리허설 | ⬜ | ⬜ | ✅ 9개 | ✅ 배포·리허설·파기 | ⬜ 실기기 |
| 09 내 정보 고치기 | ✅ | — | ✅ 14개 | ✅ | ⬜ 실기기 |
| 10 참가자 탭의 내 카드 | ✅ | — | ✅ 6개 | ✅ | ⬜ 실기기 |

> 09 는 워크플로대로 **시나리오부터** 다시 썼다 (`docs/scenarios/09-profile-edit.md`).
> 표면 문서는 따로 두지 않았다 — 입력이 `RegisterInput` 그대로고 라우트가 하나뿐이라
> 시나리오에 적힌 것이 곧 계약이다. 결정은 ADR-31 에 남겼다.

### 워크플로에서 벗어난 것 (기록)

02~07 은 **시나리오·표면 문서 없이** 구현했다. 한 번에 앱 전체를 끝내달라는 요청이었고,
문서 → 표면 → 테스트 → 구현 왕복을 슬라이스마다 도는 대신 규칙 테스트를 먼저 쓰고 바로 구현했다.

그래서 지금 상태는 이렇다.

- **규칙은 테스트가 지킨다.** 공개 범위·콕 예산·되돌리기·자리 불변식은 전부 테스트에 박혀 있다
- **범위는 문서가 지키지 못한다.** "여기 없는 건 만들지 않는다"의 근거가 01 말고는 없다.
  다음에 기능을 늘릴 때는 `docs/scenarios/NN-*.md` 부터 다시 쓸 것
- 새로 생긴 결정은 ADR-9·10·11 에 남겼다

### 구현하며 드러난 것

| 발견 | 대응 |
|---|---|
| 테스트 격리가 없다 — vitest-pool-workers 0.20 은 테스트마다 스토리지를 되돌리지 않는다 | 테스트를 파일 안에서 상태를 나눠 쓰도록 짰다. 새 테스트도 그 전제로 쓸 것 |
| 확인창이 뜨자마자 스스로 닫혔다 — 히스토리가 갱신되기 전에 "없으면 닫는다"가 돌았다 | 한 번 들어간 걸 본 뒤에만 사라짐을 닫힘으로 해석한다. 화면 테스트가 잡았다 |
| 자리 배정이 100명에 23ms (무료 플랜 10ms 초과) | 안쪽 루프에서 문자열 키를 걷어내고 typed array 로. 3ms |
| 마지막 라운드에서 상호 매칭 쌍이 갈라졌다 (동석률 50%) | 가중치가 아니라 구조로 고정 (ADR-11). 100% |

**QA 환경** — `npm run deploy:qa`. 100명 리허설은 여기서 돈다. 워커가 달라 DO 가 분리되므로
프로덕션 데이터 옆에서 가짜 인원을 만들 일이 없다.

### ⚠️ 2026-08-13~19 의 부하 실측은 전부 틀렸다 — 도구가 병목이었다

여기 있던 표들(콕 p50 7,376ms · 다시 읽기 p95 10,011ms · 운세 p50 99초, 그리고
거기서 나온 **"DO 쓰기 약 5건/초"** 라는 결론)은 **측정 도구가 만든 숫자**였다.

Node 의 `fetch` 는 원본마다 연결을 아껴 쓴다. 리허설이 한 프로세스에서 요청을 던지니
뒤엣것이 앞엣것의 연결을 기다렸다 — **서버가 아니라 스크립트가 줄을 세우고 있었다.**

같은 요청을 프로세스로 쪼개 던져서 잡았다:

| 같은 부하 | 한 프로세스 | 프로세스로 쪼갬 |
|---|---|---|
| 콕 25건 동시 | p50 4,900ms | **p50 861ms · 창 1.4초** (≈18건/초) |
| 운세 12건 동시 | p50 27,875ms | **p50 5,941ms · 창 6.7초** |

핸들러 안에서 잰 값이 결정적이었다 — 콕은 **48ms**, 운세는 **5.2초**(=LLM 시간).
서버는 내내 멀쩡했다.

> **얻은 것.** 부하 도구를 만들 때는 **도구가 병목이 아님을 먼저 증명해야 한다.**
> 요청이 200ms 일 때는 티가 안 나서 오래 못 알아챘다 — 운세처럼 한 건이 5초를 붙드는
> 경로가 들어오고 나서야 드러났다. 지금은 `setGlobalDispatcher(new Agent({connections: 256}))`
> 로 연결을 열어둔다. 파티장에서는 폰 50대가 각자 자기 연결로 온다. 도구도 그래야 한다.

### 50명 리허설 실측 (QA, 2026-08-20, 고친 도구)

`MASTER_PIN=**** PEOPLE=50 SPREAD=5 FORTUNE=30 npm run rehearsal <QA 주소>` · 실패 0

| 구간 | 결과 |
|---|---|
| 등록 50건 (동시 25) | p50 **499ms** |
| 사전 투표 전환 | 524ms |
| 다시 읽기 200건 | p50 **495ms** · p95 551ms · 최대 821ms |
| 콕 150건 (5초 분산) | p50 **495ms** · p95 546ms · 마지막 6.5초 |
| 자리 배정 (50명·12테이블) | 초안 **521ms** · 발송 547ms |
| 발표 알림 | 50/50 도달 · 첫 도달 531ms |
| **운세 50건** (30초 분산) | p50 **5,410ms** · p95 6,359ms |
| **미션 50건** | p50 **3,801ms** · p95 4,954ms |
| 앱 복귀 (조용할 때) | p50 468ms |
| 앱 복귀 (**쓰기 중**) | p50 **470ms** — 예전엔 7,400ms 로 찍혔다 |
| 소켓 재연결 50개 | p50 500ms · 50/50 성공 |

**읽기가 쓰기 뒤에 서지 않는다.** 조용할 때와 쓰기 중이 같다 (468ms vs 470ms).
출력 게이트가 읽기를 붙잡는다던 설명도 도구가 만든 그림자였다.

바닥값이 165ms 에서 ~500ms 로 올라간 건 도구 쪽이다 — 연결을 아껴 쓰지 않으니
요청마다 TLS 악수를 더 한다. 구간끼리 비교하는 데는 지장이 없다.

**50명 규모에서는 어느 구간도 줄을 서지 않는다.** 콕 상한을 낮게 두거나 도착을 흩는 것은
여전히 좋은 운영이지만, **그렇게 해야만 하는 이유는 사라졌다.**

### 클라이언트 번들은 건드리지 않았다

414 kB (gzip 132 kB). 운영자 콘솔을 따로 받아오게 갈라 봤더니 386 kB (gzip 125 kB) —
**5%** 였다. 대신 파티 도중 콘솔 덩어리를 못 받아오면 운영자가 단계를 넘기지 못한다.
참가자 5% 를 얻자고 그 실패 모드를 사지 않는다. 콘솔이 더 커지면 다시 본다.

### 읽기가 몰릴 때 실측 (QA, 50명, 1초 창, 2026-08-19)

브로드캐스트가 부르는 재조회 말고, **서버 이벤트 없이** 화면이 스스로 다시 읽는 두 경우다.
`READWINDOW=1` — 읽기 50건이 1초 안에 몰린다.

> ⚠️ 여기 있던 표와 결론(`앱 복귀·쓰기 중 8,307ms`, "읽기가 쓰기 큐 뒤에 선다",
> "시나리오 1·3·4 는 같은 문제다")은 **도구가 만든 것**이었다. 위의 정정을 보라.
> 고친 도구로는 조용할 때 468ms, 쓰기가 도는 중 470ms — **차이가 없다.**

> 요청당 DO 작업을 줄이는 최적화(등록 왕복 3→1, 콕 집계 색인화, 좌석 N+1 제거)를 먼저 했지만
> 숫자는 거의 그대로였다. 병목이 JS 가 아니라 쓰기 확정이라는 뜻이다. 그래도 되돌리지 않았다 —
> 자료가 늘수록 유리하고, 왕복이 줄면 줄 자체가 짧아진다.

**다음 할 일** — 슬라이스 08. 실기기에서 한 바퀴 돌려보고(iOS 100dvh·안드로이드 백 버튼),
100명 리허설로 WS 연결 수와 DO 지연을 재고, 회차 종료 N일 뒤 개인정보 파기 Cron 을 붙인다.

## 부록 · 두 환경

| 브랜치 | 배포처 | 머지 |
|---|---|---|
| `main` | 프로덕션 | CI 통과 + 사람이 버튼 |
| `qa` | 연습용 | CI 통과하면 자동 |

`qa` 는 언제 버려도 되는 브랜치다. 프로덕션과 멀어지면 `git push -f origin main:qa` 로 맞춘다.
